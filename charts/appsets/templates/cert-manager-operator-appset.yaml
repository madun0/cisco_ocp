apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: {{ .Values.apps.certManagerOperator.name }}
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - list:
        elements:
          - env: {{ .Values.environment | quote }}
  template:
    metadata:
      name: {{ .Values.apps.certManagerOperator.name }}
      annotations:
        argocd.argoproj.io/sync-wave: {{ .Values.apps.certManagerOperator.wave | quote }}
    spec:
      project: default
      source:
        repoURL: {{ .Values.repo.url | quote }}
        targetRevision: {{ .Values.repo.revision | quote }}
        path: {{ .Values.apps.certManagerOperator.chartPath | quote }}
        helm:
          valueFiles:
            - values.yaml
            - values-{{ .Values.environment }}.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: {{ .Values.apps.certManagerOperator.namespace | quote }}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true